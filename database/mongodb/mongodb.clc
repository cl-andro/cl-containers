# MongoDB NoSQL Document Store Sandbox Configuration
name: mongodb-server
rootfs: ./rootfs
hostname: mongodb-node
share_hardware: false

run:
    "/opt/mongodb/bin/mongod --dbpath /opt/mongodb/data --bind_ip_all --port 27017"
