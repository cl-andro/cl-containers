# dnsmasq-dns Sandbox Configuration
name: dnsmasq-dns
rootfs: ./rootfs
hostname: dnsmasq-node
share_hardware: false

run:
    "LD_PRELOAD=/opt/dnsmasq/libmock.so /usr/sbin/dnsmasq --port=1053 --user=nobody -k -C /dev/null --pid-file=/tmp/dnsmasq.pid --log-queries --log-facility=-"
