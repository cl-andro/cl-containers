# dnsmasq-dns Sandbox Configuration
name: dnsmasq-dns
rootfs: ./rootfs
hostname: dnsmasq-node
share_hardware: false

run:
    "/usr/sbin/dnsmasq --port=1053 --user=root -k -C /dev/null --log-queries --log-facility=-"
