# bind9-dns Sandbox Configuration
name: bind9-dns
rootfs: ./rootfs
hostname: bind9-node
share_hardware: false

run:
    "LD_LIBRARY_PATH=/opt/bind9/usr/lib/x86_64-linux-gnu /opt/bind9/usr/sbin/named -g -c /etc/bind/named.conf"
