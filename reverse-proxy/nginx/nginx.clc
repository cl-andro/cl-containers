# Nginx Web Server / Reverse Proxy Sandbox Configuration
name: nginx-server
rootfs: ./rootfs
hostname: nginx-node
share_hardware: false

mount_external:
    "/etc/nginx" -> "/etc/nginx"

run:
    "/usr/sbin/nginx -g 'daemon off;'"
