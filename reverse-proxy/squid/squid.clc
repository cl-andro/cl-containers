# squid-proxy Sandbox Configuration
name: squid-proxy
rootfs: ./rootfs
hostname: squid-node
share_hardware: false

mount_external:
    "/dev/shm -> /dev/shm"

run:
    "LD_LIBRARY_PATH=/opt/squid/usr/lib/x86_64-linux-gnu /opt/squid/usr/sbin/squid -N -d 1 -f /opt/squid/etc/squid/squid.conf"
