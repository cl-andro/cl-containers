# elixir-runtime Sandbox Configuration
name: elixir-runtime
rootfs: ./rootfs
hostname: elixir-node
share_hardware: false

env:
    PATH=/opt/elixir/usr/bin:/usr/bin:/bin

run:
    "/opt/elixir/usr/bin/elixir -v"
