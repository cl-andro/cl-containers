# mediawiki-app Sandbox Configuration
name: mediawiki-app
rootfs: ./rootfs
hostname: mediawiki-node
share_hardware: false

run:
    "cd /opt/mediawiki && env PHP_INI_SCAN_DIR=/opt/mediawiki /opt/frankenphp/frankenphp php-server --listen :8080 --root /opt/mediawiki"
