# trilium-notes Sandbox Configuration
name: trilium-notes
rootfs: ./rootfs
hostname: trilium-node
share_hardware: false

run:
    "cd /opt/trilium && env TRILIUM_PORT=8080 TRILIUM_HOST=0.0.0.0 TRILIUM_DATA_DIR=/opt/trilium/data ./trilium.sh"
