# fluentbit-agent Sandbox Configuration
name: fluentbit-agent
rootfs: ./rootfs
hostname: fluentbit-node
share_hardware: false

run:
    "/opt/fluentbit/opt/fluent-bit/bin/fluent-bit -i cpu -o stdout"
